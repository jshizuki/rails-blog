<%= simple_form_for [@article] do |each| %>
  <%= each.input :title %>
  <%= each.input :content %>
  <%= each.submit class: 'btn btn-primary'  %>
<% end %><br>
<%= link_to "Back", articles_path, class: "text-decoration-none link-secondary" %>
