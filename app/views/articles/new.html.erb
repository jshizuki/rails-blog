<div class="container mt-3">
  <p class="fs-2">New Article</p>
  <%= simple_form_for [@article] do |each| %>
    <%= each.input :title %>
    <%= each.input :content %>
    <%= each.submit class: 'btn btn-primary'  %>
  <% end %>
</div>
