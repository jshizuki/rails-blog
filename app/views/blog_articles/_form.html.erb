<%= simple_form_for [@article] do |each| %>
  <%= each.input :title %>
  <%= each.input :content %>
  <%= each.submit @article.new_record? ? 'Create blog' : 'Update blog', class: 'btn btn-danger mb-2'%>
<% end %>
